  - [pad()](#pad)
  - [range()](#range)

## pad()

  Left pad a string `s`
  with length `len`
  using `x` (defaults to `0`)

## range()

  Create a array using range
  starting on `s` ending on `e`
  by `step`.
